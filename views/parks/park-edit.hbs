<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-5">

            <h1>Editar {{park.name}}</h1>

            <form action="/park/edit/{{park._id}}" method="post">
                <div class="form-group">
                    <label for="name">Nombre del parque</label>
                    <input name="name" type="text" class="form-control" id="name" value="{{park.name}}">
                </div>
                <div class="form-group">
                    <label for="description">Descripción</label>
                    <input name="description" type="text" class="form-control" id="description" value="{{park.description}}">
                </div>
                <div class="form-check">
                    <input type="checkbox" class="form-check-input"  id="exampleCheck1">
                    <label name="active" class="form-check-label" for="exampleCheck1">active</label>
                </div>
                <br>

                <p>{{errorMsg}}</p>
                <button type="submit" class="btn btn-dark">Editar</button>
            </form>
        </div>
    </div>

    <a class="btn btn-dark non-radius" href="/park">Volver</a>
</div>